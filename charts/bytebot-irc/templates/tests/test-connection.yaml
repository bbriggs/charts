apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bytebot.fullname" . }}-test-connection"
  labels:
    {{- include "bytebot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bytebot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
