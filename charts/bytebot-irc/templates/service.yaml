apiVersion: v1
kind: Service
metadata:
  name: {{ include "bytebot.fullname" . }}
  labels:
    {{- include "bytebot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bytebot.selectorLabels" . | nindent 4 }}
